{
  "name": "deep-time",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "og": "node scripts/make-og.ts",
    "gate:collision": "node scripts/gate-collision.ts",
    "gate:browser": "astro build && node scripts/gate-browser.ts"
  },
  "dependencies": {
    "astro": "^5.18.2"
  },
  "devDependencies": {
    "@fontsource-variable/archivo": "^5.2.0",
    "@types/node": "^26.1.1",
    "playwright": "^1.62.1",
    "vitest": "^3.2.7"
  }
}
